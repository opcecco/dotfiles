These are some of my Linux configuration files.
To install them, just run the setup script from the repository's working directory.
If Zsh and Oh My Zsh are both installed, the script will make symbolic links in your home directory and add sources to your current .zshrc
